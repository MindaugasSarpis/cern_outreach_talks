VITE_VIDEO_REPO=MindaugasSarpis/cern_outreach_talks
# This talk owns no clips; everything serves from videos-shared. The old
# talk release (videos-2026-04-28-editai) is a frozen archive holding the
# pre-2026-07-18 HEVC encodes — keep it OUT of the runtime chain (the talk
# release is probed BEFORE shared, so it would shadow the fixed encodes).
VITE_VIDEO_RELEASE=videos-shared
VITE_VIDEO_SHARED_RELEASE=videos-shared
